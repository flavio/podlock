name: Create a new release for podlock stack

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build:
    permissions:
      packages: write # Pushing images to ghcr.io
      id-token: write # Signing images with cosign
    uses: ./.github/workflows/container-build.yml
    with:
      version: ${{ github.ref_name }}
    secrets: inherit
  create-release:
    needs: [build]
    permissions:
      contents: write # Creating a release
    runs-on: ubuntu-latest
    steps:
      - name: Retrieve tag name
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        run: |
          echo TAG_NAME=$(echo ${{ github.ref_name }}) >> $GITHUB_ENV
      - name: Get release ID from the release created by release drafter
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8.0.0
        with:
          script: |
            let releases = await github.rest.repos.listReleases({
                owner: context.repo.owner,
                repo: context.repo.repo,
            });
            for (const release of releases.data) {
              if (release.draft) {
                      core.info(release)
                      core.exportVariable('RELEASE_ID', release.id)
                      return
              }
            }
            core.setFailed(`Draft release not found`)
      - name: Download attestation artifacts
        uses: actions/download-artifact@018cc2cf5baa6db3ef3c5f8a56943fffe632ef53 # v6.0.0
        with:
          pattern: attestation-podlock-*
          merge-multiple: true
      - name: Display structure of downloaded files
        run: ls -R

      - name: Upload release assets
        id: upload_release_assets
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8.0.0
        with:
          script: |
            let fs = require('fs');
            let path = require('path');
            let files = [
                'podlock-controller-attestation-amd64-provenance.intoto.jsonl',
                'podlock-controller-attestation-amd64-provenance.intoto.jsonl.bundle.sigstore',
                'podlock-controller-attestation-arm64-provenance.intoto.jsonl',
                'podlock-controller-attestation-arm64-provenance.intoto.jsonl.bundle.sigstore',
                'podlock-controller-attestation-amd64-sbom.json',
                'podlock-controller-attestation-amd64-sbom.json.bundle.sigstore',
                'podlock-controller-attestation-arm64-sbom.json',
                'podlock-controller-attestation-arm64-sbom.json.bundle.sigstore',

                'podlock-nri-attestation-amd64-provenance.intoto.jsonl',
                'podlock-nri-attestation-amd64-provenance.intoto.jsonl.bundle.sigstore',
                'podlock-nri-attestation-arm64-provenance.intoto.jsonl',
                'podlock-nri-attestation-arm64-provenance.intoto.jsonl.bundle.sigstore',
                'podlock-nri-attestation-amd64-sbom.json',
                'podlock-nri-attestation-amd64-sbom.json.bundle.sigstore',
                'podlock-nri-attestation-arm64-sbom.json',
                'podlock-nri-attestation-arm64-sbom.json.bundle.sigstore',
                ]
            const {RELEASE_ID} = process.env
            for (const file of files) {
              let file_data = fs.readFileSync(file);
              let response = await github.rest.repos.uploadReleaseAsset({
                owner: context.repo.owner,
                repo: context.repo.repo,
                release_id: `${RELEASE_ID}`,
                name: path.basename(file),
                data: file_data,
              });
            }
      - name: Publish release
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8.0.0
        with:
          script: |
            const {RELEASE_ID} = process.env
            const {TAG_NAME} = process.env
            isPreRelease = ${{ contains(github.ref_name, '-alpha') || contains(github.ref_name, '-beta') || contains(github.ref_name, '-rc') }}
            github.rest.repos.updateRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              release_id: `${RELEASE_ID}`,
              draft: false,
              tag_name: `${TAG_NAME}`,
              name: `podlock ${TAG_NAME}`,
              prerelease: isPreRelease,
              make_latest: !isPreRelease
            });
